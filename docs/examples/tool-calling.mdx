---
title: Tool calling example
description: Multiple tools, run events, and trace spans.
---

Shows an agent with multiple tools (`add` and `celsius_to_fahrenheit`) and how Harbor executes tool calls inside `Runtime.run()`.

## Run

```bash
pnpm --filter @harbor/example-tool-calling start
```

## What it demonstrates

- Registering more than one tool on an `Agent`
- Observing `tool.start` / `tool.end` via `onEvent`
- Reading the final answer and run trace from `RunResult`

## Snippet

```ts
import { Agent, Runtime, type JsonObject, type Tool } from "@harbor/core";
import { OpenAIProvider } from "@harbor/providers";

const add: Tool = {
  name: "add",
  description: "Add two numbers.",
  parameters: {
    type: "object",
    properties: { a: { type: "number" }, b: { type: "number" } },
    required: ["a", "b"],
  },
  execute(args: JsonObject) {
    return Number(args["a"]) + Number(args["b"]);
  },
};

const celsiusToFahrenheit: Tool = {
  name: "celsius_to_fahrenheit",
  description: "Convert Celsius to Fahrenheit.",
  parameters: {
    type: "object",
    properties: { celsius: { type: "number" } },
    required: ["celsius"],
  },
  execute(args: JsonObject) {
    return (Number(args["celsius"]) * 9) / 5 + 32;
  },
};

const result = await new Runtime().run({
  agent: new Agent({
    name: "tools",
    provider: new OpenAIProvider({ model: "gpt-4o-mini" }),
    instructions: "Prefer calling tools over estimating.",
    tools: [add, celsiusToFahrenheit],
  }),
  input: "What is 19 + 23, and what is 19°C in Fahrenheit?",
  onEvent(event) {
    if (event.type === "tool.start") {
      console.log("calling:", event.toolCall.name, event.toolCall.arguments);
    }
  },
});

console.log(result.output?.content);
console.log("trace spans:", result.trace?.spans.length ?? 0);
```

Source: [`examples/tool-calling`](https://github.com/harshlocham/harbor/tree/main/examples/tool-calling)
