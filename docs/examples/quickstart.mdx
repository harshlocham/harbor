---
title: Quickstart example
description: Smallest end-to-end Harbor agent with one tool.
---

The smallest end-to-end Harbor example: an `Agent` with `OpenAIProvider`, one tool, `Runtime.run()`, and printing the result.

## Run

```bash
pnpm install && pnpm build
export OPENAI_API_KEY=sk-...
pnpm --filter @harbor/example-quickstart start
```

## What it demonstrates

- Constructing `OpenAIProvider` (reads `OPENAI_API_KEY`)
- Registering a tool on an `Agent`
- Running the agent loop with `Runtime.run()`
- Reading `RunResult.status` and `RunResult.output`

## Snippet

```ts
import { Agent, Runtime, type Tool } from "@harbor/core";
import { OpenAIProvider } from "@harbor/providers";

const getTime: Tool = {
  name: "get_time",
  description: "Return the current UTC time as an ISO-8601 string.",
  parameters: { type: "object", properties: {} },
  execute() {
    return new Date().toISOString();
  },
};

const result = await new Runtime().run({
  agent: new Agent({
    name: "quickstart",
    provider: new OpenAIProvider({ model: "gpt-4o-mini" }),
    instructions: "When asked for the time, call get_time and answer briefly.",
    tools: [getTime],
  }),
  input: "What time is it in UTC right now?",
});

console.log(result.output?.content);
```

Source: [`examples/quickstart`](https://github.com/harshlocham/harbor/tree/main/examples/quickstart)
