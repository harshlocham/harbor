# Changesets

Hello! This folder stores [Changesets](https://github.com/changesets/changesets) for Harbor package versioning.

Run `pnpm changeset` from the repo root to add a changeset describing your change.
